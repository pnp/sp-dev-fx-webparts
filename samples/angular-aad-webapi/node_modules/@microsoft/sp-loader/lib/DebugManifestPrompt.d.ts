/// <reference types="es6-promise" />
export default class DebugManifestPrompt {
    private static _instance;
    private _allowDebug;
    static readonly instance: DebugManifestPrompt;
    peek(): boolean;
    dangerouslyForce(): void;
    ensureDebugAllowed(ask: boolean): Promise<boolean>;
}
