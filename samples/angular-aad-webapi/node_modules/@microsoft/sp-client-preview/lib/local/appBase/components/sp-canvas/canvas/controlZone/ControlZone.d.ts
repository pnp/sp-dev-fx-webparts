import * as React from 'react';
import { ICanvasContext } from '../canvasComponent/ICanvasComponent';
import { IControlZoneProps } from './IControlZone';
export default class ControlZone extends React.Component<IControlZoneProps, void> {
    static contextTypes: React.ValidationMap<ControlZone>;
    /**
     * @ readOnly
     */
    private static _VERTICAL_MARGIN;
    context: ICanvasContext;
    private _childControl;
    private _childControlElement;
    private _domElement;
    private _toolbarElement;
    private _trapDiv;
    constructor(props: IControlZoneProps, context: ICanvasContext);
    shouldComponentUpdate(nextProps: IControlZoneProps): boolean;
    componentDidMount(): void;
    componentDidUpdate(prevProps: IControlZoneProps): void;
    render(): React.ReactElement<IControlZoneProps>;
    private _handleControlFocus(e);
    private _handleClickCapture(e);
    private _handleKeydownCapture(evt);
    private _handleToolbarKeydown(evt);
    private _handleMouseOut(e);
    private _handleMouseOver(e);
    private _handleTrapFocus(e);
    private _selectControl();
}
