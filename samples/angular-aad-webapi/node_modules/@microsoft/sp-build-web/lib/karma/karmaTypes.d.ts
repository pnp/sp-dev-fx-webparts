/// <reference types="webpack" />
import { ConfigOptions } from 'karma';
import { Configuration } from 'webpack';
export interface IExtendedKarmaConfig extends ConfigOptions {
    webpack: Configuration;
    webpackMiddleware: any;
    testResultReporter: IReporterConfig;
    coverageReporter: any;
}
export interface IReporterConfig {
    outputFile: string;
}
