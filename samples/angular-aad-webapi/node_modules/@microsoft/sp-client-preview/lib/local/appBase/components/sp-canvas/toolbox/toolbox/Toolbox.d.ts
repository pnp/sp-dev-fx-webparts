import * as React from 'react';
import { ICanvasContext } from '../../canvas/canvasComponent/ICanvasComponent';
import { IToolboxProps, IToolboxState } from './IToolbox';
import './Toolbox.scss';
export default class Toolbox extends React.Component<IToolboxProps, IToolboxState> {
    static contextTypes: React.ValidationMap<Toolbox>;
    context: ICanvasContext;
    private _domNode;
    constructor(props: IToolboxProps, context: ICanvasContext);
    componentDidMount(): void;
    componentDidUpdate(prevProps: IToolboxProps): void;
    componentWillReceiveProps(nextProps: IToolboxProps): void;
    render(): React.ReactElement<IToolboxProps>;
    private _createSectionItemProps(controlType);
    private _generateSectionItems(onItemClick);
    private _handleKeyDownCapture(e);
}
