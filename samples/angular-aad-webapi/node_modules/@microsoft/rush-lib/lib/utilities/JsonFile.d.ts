/**
 * Utilities for reading/writing JSON files.
 */
export default class JsonFile {
    static loadJsonFile(jsonFilename: string): any;
    static saveJsonFile(jsonData: any, jsonFilename: string): void;
}
