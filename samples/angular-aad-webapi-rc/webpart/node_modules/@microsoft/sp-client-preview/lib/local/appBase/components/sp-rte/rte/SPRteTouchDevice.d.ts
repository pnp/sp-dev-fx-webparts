/**
 * @copyright Microsoft Corporation. All rights reserved.
 */
import SPRte, { ISPRteProps, ISPRteState } from './SPRte';
import { ISPRteFormattingBarPosition } from '../formattingbar/SPRteFormattingBar';
import { IRichTextEditor } from './IRichTextEditor';
import { IRange } from './IRange';
export interface ISPRteTouchDeviceProps extends ISPRteProps {
}
export interface ISPRteTouchDeviceState extends ISPRteState {
}
/**
 * @class SPRteTouchDevice class. This component is used as a canvas control for touch devices.
 */
export default class SPRteTouchDevice extends SPRte {
    private static _getFormattingBarDefaultPosition(isRtl?);
    constructor(props: ISPRteTouchDeviceProps);
    componentWillUnmount(): void;
    protected _afterEditorCreated(editor: IRichTextEditor): void;
    protected _initState(): void;
    protected _getFormattingBarPosition(): ISPRteFormattingBarPosition;
    protected _handleScroll(): void;
    protected _handleSelectionChange(range: IRange, forceUpdate?: boolean): void;
}
