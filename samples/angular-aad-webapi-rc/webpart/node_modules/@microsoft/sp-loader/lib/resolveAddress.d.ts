import { IClientSideComponentManifest } from '@microsoft/sp-module-interfaces';
/**
 * Given a manifest and a resource name (for a path/localizedPath dependency)
 * return the full URL to the resource
 */
export default function resolveAddress(manifest: IClientSideComponentManifest, resourceName: string): string;
