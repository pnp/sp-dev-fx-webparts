import { GulpTask } from '@microsoft/gulp-core-build';
import gulp = require('gulp');
export declare class ReloadTask extends GulpTask<{}> {
    name: string;
    executeTask(gulp: gulp.Gulp, completeCallback?: (error?: string) => void): void;
}
