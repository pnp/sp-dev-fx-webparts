// Defines the settings for the Retail Dashboard application
export interface RetailSettings {
    // The unique ID of the SharePoint Online site where the data are stored
    siteId: string;
    // The server relative URL of the SharePoint Online site where the data are stored
    serverRelativeUrl: string; 
}
