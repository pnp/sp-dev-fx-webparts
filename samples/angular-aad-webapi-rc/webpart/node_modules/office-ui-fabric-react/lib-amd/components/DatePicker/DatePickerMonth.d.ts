import * as React from 'react';
import { IDatePickerStrings } from './DatePicker.Props';
export interface IDatePickerMonthProps {
    navigatedDate: Date;
    strings: IDatePickerStrings;
    onNavigateDate: (date: Date, focusOnNavigatedDay: boolean) => void;
}
export declare class DatePickerMonth extends React.Component<IDatePickerMonthProps, {}> {
    private _selectMonthCallbacks;
    constructor(props: IDatePickerMonthProps);
    render(): JSX.Element;
    private _onKeyDown(callback, ev);
    private _onSelectNextYear();
    private _onSelectPrevYear();
    private _onSelectMonth(newMonth);
}
