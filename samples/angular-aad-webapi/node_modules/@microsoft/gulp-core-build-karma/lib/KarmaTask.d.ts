import { GulpTask, IBuildConfig } from '@microsoft/gulp-core-build';
import * as gulp from 'gulp';
export interface IKarmaTaskConfig {
    configPath: string;
    /**
     * If specified, a "tests.js" file will be created in the temp folder using
     *  this RegExp to locate test files.
     */
    testMatch?: RegExp | string;
}
export declare class KarmaTask extends GulpTask<IKarmaTaskConfig> {
    name: string;
    taskConfig: IKarmaTaskConfig;
    readonly resources: Object;
    private _resources;
    loadSchema(): Object;
    getCleanMatch(buildConfig: IBuildConfig, taskConfig?: IKarmaTaskConfig): string[];
    isEnabled(buildConfig: IBuildConfig): boolean;
    executeTask(gulp: gulp.Gulp, completeCallback: (error?: Error | string) => void): void;
}
