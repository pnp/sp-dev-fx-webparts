/**
 * Defines the Retail Return Reasons stats
 */
export interface RetailReturnReasonsStats {
    
    // The number of returns for the reason "Incorrect Fit"
    incorrectFit: number;

    // The number of returns for the reason "Defective"
    defective: number;

    // The number of returns for the reason "Wrong Item"
    wrongItem: number;

    // The number of returns for the reason "Dislike"
    disliked: number;

    // The number of returns for the reason "Wrong Size"
    wrongSize: number;
}
