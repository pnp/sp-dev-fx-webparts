import { DisplayMode } from '@microsoft/sp-core-library';
import * as React from 'react';
import CanvasControlType from '../../canvas/canvasControl/CanvasControlType';
import { IControlPosition } from '../../canvas/canvasLayout/ICanvasLayout';
import Toolbar from './Toolbar';
export interface IToolbarProps extends React.Props<Toolbar> {
    controlType: CanvasControlType;
    displayMode: DisplayMode;
    id: string;
    row: number;
    onDeleteButtonClick: (row: number, layout: IControlPosition) => void;
    onConfigureButtonClick: (id: string) => void;
    className?: string;
    controlPosition?: IControlPosition;
}
