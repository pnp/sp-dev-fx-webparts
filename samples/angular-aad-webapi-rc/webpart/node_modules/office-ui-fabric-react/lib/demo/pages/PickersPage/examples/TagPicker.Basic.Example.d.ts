import * as React from 'react';
export declare class TagPickerBasicExample extends React.Component<{}, {}> {
    render(): JSX.Element;
    private _onFilterChanged(filterText, tagList);
    private _listContainsDocument(tag, tagList);
}
