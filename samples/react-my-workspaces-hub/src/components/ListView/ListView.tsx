import { ListViewInner } from './ListViewInner';

/**
 * Generic, virtualized list view with sorting, choice/range filtering, frozen
 * columns, typed cell renderers, a column chooser, and built-in CSV/Excel export.
 */
export const ListView = ListViewInner;
