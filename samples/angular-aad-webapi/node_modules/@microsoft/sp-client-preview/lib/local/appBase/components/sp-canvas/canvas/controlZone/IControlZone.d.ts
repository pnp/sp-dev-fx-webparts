import * as React from 'react';
import { DisplayMode } from '@microsoft/sp-core-library';
import { IControlPosition } from '../canvasLayout/ICanvasLayout';
export interface IControlZoneProps extends React.Props<React.StatelessComponent<IControlZoneProps>> {
    displayMode: DisplayMode;
    id: string;
    isSelected: boolean;
    row: number;
    position?: IControlPosition;
}
