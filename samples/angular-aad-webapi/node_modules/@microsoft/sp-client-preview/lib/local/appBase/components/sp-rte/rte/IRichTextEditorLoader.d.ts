/// <reference types="es6-promise" />
/**
 * @copyright Microsoft Corporation. All rights reserved.
 */
/**
 * Interface for RTE async module loader
 */
export interface IRichTextEditorLoader {
    /**
     * Initiates an async request to load the module and returns the promise
     * @param editorSrc represents an optional editor source override
     */
    loadModule(editorSrc?: string): Promise<any>;
}
