import { IWebPartManagerContext } from '@microsoft/sp-webpart-base';
export default class CanvasRenderPerformance {
    private _renderedWebparts;
    private _webPartsRenderedStatus;
    private _handleRenderingDone;
    private _allWebPartsAdded;
    private _constructionTime;
    static getPerformanceTag(alias: string, instanceId: string): string;
    constructor(handleRenderingDone: (loadTimeMS: number) => void);
    addWebpart(webpartId: string): void;
    doneAddingWebparts(): void;
    webPartRendered(context: IWebPartManagerContext): void;
    private _onAllWebpartsRendered();
    private _haveAllWebpartsRendered();
}
