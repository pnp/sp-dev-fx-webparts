Hugo site scaffold generated by mkdocs_to_hugo.py

Next steps:
1. Install Hugo (https://gohugo.io/) and the Materials theme for Hugo.
2. Move or adjust theme files into `themes/hugo-materials`.
3. Run `hugo server -D` inside `hugo_site` to preview.