/**
 * @copyright Microsoft Corporation. All rights reserved.
 */
import { IRichTextFormat } from './IRichTextFormat';
import { IRichTextEditorConfiguration } from './IRichTextEditorConfiguration';
/**
 * Static class for SPRte configuratoion
 * Note: We assume all RTE instances share the same configuration by design
 * Should this change in the future, we'll need to change this to non-static design
 */
export default class SPRteCKEditorConfiguration implements IRichTextEditorConfiguration {
    private _supportedFormatsArray;
    private _formats;
    constructor();
    readonly editorElementClassName: string;
    readonly formats: Map<string, IRichTextFormat>;
    readonly supportedFormatsArray: IRichTextFormat[];
}
