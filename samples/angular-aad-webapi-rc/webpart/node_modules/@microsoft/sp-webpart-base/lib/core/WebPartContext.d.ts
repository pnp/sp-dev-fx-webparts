import { IClientSideWebPartManifestInstance } from '@microsoft/sp-module-interfaces';
import { HttpClient, SPHttpClient } from '@microsoft/sp-http';
import { PageContext } from '@microsoft/sp-page-context';
import { _ClientSideComponentContext } from '@microsoft/sp-component-base';
import IClientSideWebPartStatusRenderer from './/IClientSideWebPartStatusRenderer';
import { IEventAggregator } from './events/IEvent';
import IWebPartContext from './IWebPartContext';
import IWebPartHost from './IWebPartHost';
import { IPropertyPaneAccessor } from './context/IPropertyPaneAccessor';
import IWebPartContextParameters from './IWebPartContextParameters';
/**
 * Web part context object. This object contains the contextual services available to a web part. e.g.
 * a contextual instance to the http client.
 *
 * PRIVATE - The WebPartContext class is not exported from index.ts. IWebPartContext is the public interface.
 */
export default class WebPartContext extends _ClientSideComponentContext implements IWebPartContext {
    private _instanceId;
    private _webPartTag;
    private _domElement;
    private _spHttpClient;
    private _httpClient;
    private _pageContext;
    private _statusRenderer;
    private _eventAggregator;
    private _setPropertyPaneState;
    private _isPropertyPaneRenderedByWebPart;
    private _host;
    private _propertyPaneAccessor;
    constructor(parameters: IWebPartContextParameters);
    /**
     * @see IWebPartContext
     * @internalremarks
     * This augments the type from IClientSideComponentContext.manifest.
     */
    readonly manifest: IClientSideWebPartManifestInstance<any>;
    /**
     * @see IWebPartContext
     */
    readonly instanceId: string;
    /**
     * @see IWebPartContext
     */
    readonly webPartTag: string;
    /**
     * Reference to the DOM element that hosts this client side component.
     */
    readonly domElement: HTMLElement;
    /**
     * @see IWebPartContext
     */
    readonly spHttpClient: SPHttpClient;
    /**
     * @see IWebPartContext
     */
    readonly httpClient: HttpClient;
    /**
     * @see IWebPartContext
     */
    readonly pageContext: PageContext;
    /**
     * @see IWebPartContext
     */
    readonly statusRenderer: IClientSideWebPartStatusRenderer;
    /**
     * @see IWebPartContext
     */
    readonly eventAggregator: IEventAggregator;
    /**
     * @see IWebPartContext
     */
    readonly propertyPane: IPropertyPaneAccessor;
    /**
     * @see IWebPartContext
     */
    readonly host: IWebPartHost;
    /** @inheritdoc */
    protected onProvideServices(parameters: IWebPartContextParameters): void;
    /** @inheritdoc */
    protected onConsumeServices(): void;
}
