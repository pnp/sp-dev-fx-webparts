"use strict";
var MockToolbarButtonProps = (function () {
    function MockToolbarButtonProps() {
    }
    MockToolbarButtonProps.getToolbarButtons = function (count) {
        var toolbarButtonMocks = [];
        var onClick = sinon.spy();
        var onKeyDown = sinon.spy();
        for (var ct = 0; ct < count; ct++) {
            var strCt = ct.toString();
            toolbarButtonMocks.push({
                onClick: onClick,
                disabled: false,
                isActive: true,
                key: strCt + '-key',
                tabIndex: 0,
                onKeyDown: onKeyDown,
                automationId: strCt + '-id',
                title: strCt + '-title'
            });
        }
        return toolbarButtonMocks;
    };
    return MockToolbarButtonProps;
}());
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = MockToolbarButtonProps;
