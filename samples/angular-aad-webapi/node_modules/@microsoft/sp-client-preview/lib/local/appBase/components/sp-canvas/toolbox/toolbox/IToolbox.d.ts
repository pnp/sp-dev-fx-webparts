import * as React from 'react';
import ICanvasControl from '../../canvas/canvasControl/ICanvasControl';
import { IToolboxItem } from '../toolboxItem/IToolboxItem';
import { IControlPosition } from '../../canvas/canvasLayout/ICanvasLayout';
import Toolbox from './Toolbox';
export interface IToolboxProps extends React.Props<Toolbox> {
    isLoading: boolean;
    isOpen: boolean;
    onItemClick: (itemProps: ICanvasControl) => void;
    row: number;
    toolboxItems: IToolboxItem[];
    verticalPosition: number;
    horizontalPosition?: number;
    errorMessage?: string;
    controlPosition?: IControlPosition;
}
export interface IToolboxState {
    horizontalPosition?: number;
    verticalPosition?: number;
}
