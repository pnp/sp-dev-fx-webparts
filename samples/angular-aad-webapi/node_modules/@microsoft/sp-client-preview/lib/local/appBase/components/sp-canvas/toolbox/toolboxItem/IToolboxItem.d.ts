import * as React from 'react';
import ICanvasControl from '../../canvas/canvasControl/ICanvasControl';
export interface IToolboxItem extends React.Props<React.StatelessComponent<IToolboxItemProps>> {
    description: string;
    displayName: string;
    itemProps: ICanvasControl;
    msIconName: string;
}
export interface IToolboxItemProps extends IToolboxItem {
    onClick: (itemProps: ICanvasControl) => void;
    automationId?: string;
}
