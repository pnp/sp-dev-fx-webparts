/**
 * @Copyright (c) Microsoft Corporation.  All rights reserved.
 *
 * Utility code that does not fit in any other bucket.
 */
import { _ILogSource } from '@ms/sp-telemetry';
import { SPError } from '@microsoft/sp-core-library';
/**
 * Execute the passed in delegate with a guarantee that no exception will leak out.
 * Do not use this normally. Only in error handling scenarios where you would like
 * to be defesive.
 * @param {method} method to execute
 * @apram {logSource} log source
 */
export declare function executeWithoutFailing(method: () => void, logSource: _ILogSource): void;
/**
 * Execute the passed in delegate and re-throw a custom error.
 * @param {method} method to execute.
 * @param {errotToThrow} custom error to throw.
 * @apram (optional) {logSource} log source.
 */
export declare function executeAndReThrow(method: () => void, errorToThrow: SPError, logSource?: _ILogSource): void;
