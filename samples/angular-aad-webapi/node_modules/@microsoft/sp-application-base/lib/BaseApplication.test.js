"use strict";
var chai_1 = require("chai");
describe('BaseApplication tests', function () {
    describe('Testing that we can test things', function () {
        it('should have been constructed', function (done) {
            chai_1.assert.isObject({});
            done();
        });
    });
});
