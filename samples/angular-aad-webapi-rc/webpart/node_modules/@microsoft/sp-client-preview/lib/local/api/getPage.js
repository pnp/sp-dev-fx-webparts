"use strict";
var fs = require('fs');
var path = require('path');
var rootDir = process.cwd();
function getPage(req, res) {
    'use strict';
    if (req.id) {
        var file = path.join(rootDir, 'data', req.id, 'page.data');
        var pageData = fs.readFileSync(file, 'utf8');
        res.json({ id: req.id, data: pageData });
    }
}
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = getPage;
