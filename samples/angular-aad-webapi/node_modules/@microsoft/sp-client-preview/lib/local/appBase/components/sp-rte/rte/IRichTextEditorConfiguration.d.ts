import { IRichTextFormat } from './IRichTextFormat';
export interface IRichTextEditorConfiguration {
    editorElementClassName: string;
    formats: Map<string, IRichTextFormat>;
    supportedFormatsArray: IRichTextFormat[];
    editorSource?: string;
}
