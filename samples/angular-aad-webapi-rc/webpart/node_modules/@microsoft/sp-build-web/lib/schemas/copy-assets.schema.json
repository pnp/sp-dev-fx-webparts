{
  "title": "Copy Assets Task Configuration",
  "description": "Defines parameters for the task which copies assets",

  "type": "object",
  "additionalProperties": false,
  "properties": {
    "deployCdnPath": {
      "title": "Path to CDN",
      "description": "Path to where assets should be staged for deployment to CDN during a production build",
      "type": "string"
    },

    "extsToIgnore": {
      "title": "Extensions to Ignore",
      "description": "Extensions of webpack-produced assets to ignore",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}