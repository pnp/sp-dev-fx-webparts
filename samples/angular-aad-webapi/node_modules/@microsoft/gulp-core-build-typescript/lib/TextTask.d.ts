/// <reference types="node" />
import { GulpTask } from '@microsoft/gulp-core-build';
import gulpType = require('gulp');
export interface ITextTaskConfig {
    /**
     * Glob matches for files that should be converted into modules.
     */
    textMatch?: string[];
}
export declare class TextTask extends GulpTask<ITextTaskConfig> {
    name: string;
    taskConfig: ITextTaskConfig;
    executeTask(gulp: gulpType.Gulp): NodeJS.ReadWriteStream;
}
