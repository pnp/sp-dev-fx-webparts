/// <reference types="es6-promise" />
import IXml from '../models/packageXml/IXml';
/** Loads a directory of files into IXml objects */
export declare function getFiles(cwd: string, globs: string, foundMessage?: string): Promise<IXml[]>;
/** Reads the list of relative path from a specified folder */
export declare function getRelativeFilePaths(cwd: string, globs: string, foundMessage?: string): Promise<string[]>;
