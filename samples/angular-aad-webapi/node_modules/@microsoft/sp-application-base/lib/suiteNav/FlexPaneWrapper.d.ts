import { _LogSource } from '@ms/sp-telemetry';
export default class FlexPaneWrapper {
    static logSource: _LogSource;
    OnFlexPaneVisibilityChanged(callback: (visible: boolean) => any): number;
    OpenFlexPaneForProvider(provider: string): void;
    /**
     * Create a log entry for logging
     * @param moduleName represents the module that the logger was called in
     * @param errorMessage represents the error string that occured
     * @returns a SuiteNav Error Log Entry
     */
    private _createFlexPaneWrapperErrorLogEntry(moduleName, errorMessage);
}
