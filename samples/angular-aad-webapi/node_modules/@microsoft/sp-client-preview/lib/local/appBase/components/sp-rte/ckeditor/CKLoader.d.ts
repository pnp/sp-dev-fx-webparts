/// <reference types="es6-promise" />
import { IRichTextEditorLoader } from '../rte/IRichTextEditorLoader';
export default class CKLoader implements IRichTextEditorLoader {
    private static logSource;
    private static _editorPromise;
    private static _editorModule;
    loadModule(editorSrc?: string): Promise<CKEDITOR.editor>;
}
