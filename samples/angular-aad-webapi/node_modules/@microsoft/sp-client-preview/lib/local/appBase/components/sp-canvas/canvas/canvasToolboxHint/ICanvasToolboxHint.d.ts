import { IControlPosition } from '../canvasLayout/ICanvasLayout';
export interface ICanvasToolboxHintProps {
    ariaLabel: string;
    row: number;
    shouldFocus?: boolean;
    isVisible?: boolean;
    layout?: IControlPosition;
    shouldCenterAlign?: boolean;
}
