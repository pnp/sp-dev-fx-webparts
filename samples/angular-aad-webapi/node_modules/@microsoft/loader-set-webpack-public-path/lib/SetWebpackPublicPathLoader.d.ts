export interface ISetWebpackPublicPathLoaderOptions {
    systemJs?: boolean;
    scriptPath?: string;
    urlPrefix?: string;
    publicPath?: string;
}
export declare class SetWebpackPublicPathLoader {
    static registryVarName: string;
    private static defaultOptions;
    static getGlobalRegisterCode(debug?: boolean): string;
    static setOptions(options: ISetWebpackPublicPathLoaderOptions): void;
    static pitch(remainingRequest: string): string;
    private static joinLines(lines);
    private static escapeSingleQuotes(str);
    private static appendSlashAndEscapeSingleQuotes(str);
    private static getOptions(query);
    constructor();
}
