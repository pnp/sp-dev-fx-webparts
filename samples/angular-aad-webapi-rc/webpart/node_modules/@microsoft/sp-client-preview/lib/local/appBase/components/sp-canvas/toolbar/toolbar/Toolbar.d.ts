import * as React from 'react';
import { IToolbarProps } from './IToolbar';
import './Toolbar.scss';
export default class Toolbar extends React.Component<IToolbarProps, void> {
    constructor(props: IToolbarProps);
    render(): React.ReactElement<IToolbarProps>;
    private _handleConfigureButtonClicked();
    private _handleDeleteButtonClicked();
}
